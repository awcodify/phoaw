language: elixir

elixir:
  - 1.5.0

addons:
  postgresql: '9.4'

services:
  - postgresql

before_script:
  - cp config/travis.exs config/test.secret.exs
  - mix deps.get
  - mix do ecto.create, ecto.migrate

script:
  - mix test
